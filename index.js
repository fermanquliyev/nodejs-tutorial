const generateRandomNumber = require('./utils').generateRandomNumber;

const celciusToFahrenheit = require('./utils').celciusToFahrenheit;

console.log(generateRandomNumber());
console.log(celciusToFahrenheit(25));